name: Keep Streamlit Awake

on:
  schedule:
    # GitHub Actions cron saatleri UTC'dir. Her 6 saatte bir çalışır.
    - cron: "17 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: keep-streamlit-awake
  cancel-in-progress: true

jobs:
  visit-app:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check application URL
        env:
          APP_URL: ${{ secrets.STREAMLIT_APP_URL }}
        shell: bash
        run: |
          if [ -z "$APP_URL" ]; then
            echo "::error::STREAMLIT_APP_URL secret is not defined."
            exit 1
          fi
          if [[ "$APP_URL" != https://*.streamlit.app* ]]; then
            echo "::error::STREAMLIT_APP_URL must be a https://...streamlit.app address."
            exit 1
          fi

      - name: Visit Streamlit health endpoint
        env:
          APP_URL: ${{ secrets.STREAMLIT_APP_URL }}
        shell: bash
        run: |
          APP_URL="${APP_URL%/}"
          HEALTH_URL="${APP_URL}/_stcore/health"

          curl \
            --fail \
            --location \
            --max-redirs 5 \
            --retry 3 \
            --retry-all-errors \
            --connect-timeout 20 \
            --max-time 60 \
            --user-agent "Mozilla/5.0 (compatible; EVENTIX-Keep-Awake/1.0)" \
            --silent \
            --show-error \
            "$HEALTH_URL"
